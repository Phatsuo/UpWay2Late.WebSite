/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LinkedInShareComponent } from './linked-in-share.component';

describe('Component: LinkedInShare', () => {
  it('should create an instance', () => {
    let component = new LinkedInShareComponent(null, null);
    expect(component).toBeTruthy();
  });
});
